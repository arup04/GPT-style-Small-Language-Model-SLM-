# 🧠 GPT-style Small Language Model (SLM)

## 🚀 Overview

This project implements a **GPT-style Transformer-based Small Language Model (SLM)** from scratch using PyTorch.

The model is trained to understand and generate text using a decoder-only Transformer architecture similar to GPT.

